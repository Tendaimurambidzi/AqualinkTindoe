{
  "name": "drift-backend-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "build": "echo 'JS functions - no build step'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^4.4.1",
    "adm-zip": "^0.5.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
