rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to all files for authenticated users
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // Special rules for user-specific content
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    match /media/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    match /premium_docs/{roomId}/{fileName} {
      allow create: if request.auth != null && request.resource.contentType == 'application/pdf';
      allow read: if request.auth != null;
      allow update, delete: if request.auth != null;
    }

    match /premium_presentations/{roomId}/{fileName} {
      allow create, read, update, delete: if request.auth != null;
    }
  }
}
